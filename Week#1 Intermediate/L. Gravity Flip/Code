#include <algorithm>
#include <iostream>

using namespace std;

int main() {

    int length;

    cin >> length;
    int arr[length];

    for (int i = 0; i < length; i++) {

        cin >> arr[i];

    }

    sort(arr, arr + length);

    for (int temp:arr)
        cout << temp << " ";


}
